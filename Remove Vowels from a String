class Solution:
    def removeVowels(self, s: str) -> str:
        result=''
        vowels = ['a','e','i','o','u']
        for i in s:
            if i in vowels:
                continue
            result += i

        return result
